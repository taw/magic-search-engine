.row
  .col-12
    %h3 Magic Sets
    %ul
      - @sets.each do |set|
        %li
          = link_to_set(set) do
            = "#{set.name}"
          = "(#{set.code.upcase}, #{set.printings.size} cards)"
