# Foundations Jumpstart, the Jumpstart set that came out alongside Foundations.
# Its set name already ends in Jumpstart, so the format names itself.
jumpstart:
  name: "{set_name}"
  boosters:
  - 2x j25-jumpstart
